Entities\Route:
    repository: Entities\Repositories\Route
    table: routes

    plugins:
        Date:

    fields:
        idRoute:
            isKey: true
        name:
        pattern:
        module:
        controller:
        action:
        requirements:
        methods:
        isLocalized:
        isDefault:
        isEnabled:
        dateCreated:
        dateUpdated:
        idApplication:
        application:
            target: Entities\Application
            relation: one_to_one
            fromField: idApplication
            toField: idApplication
            owner: false