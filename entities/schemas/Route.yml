Entities\Route:
    repository: Entities\Repositories\Route
    table: routes

    plugins:
        Date:

    fields:
        id:
            isKey: true
        name:
        pattern:
        module:
        controller:
        action:
        requirements:
        methods:
        isEnabled:
        dateCreated:
        dateUpdated:
        application_id:
        application:
            target: Entities\Application
            relation: one_to_one
            fromField: application_id
            toField: id